services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dynexa
      POSTGRES_PASSWORD: dynexa
      POSTGRES_DB: dynexa
    ports:
      - "5432:5432"
    volumes:
      - dynexa_pg:/var/lib/postgresql/data

volumes:
  dynexa_pg:
